{
  "name": "Edi",
  "default_attributes": { },
  "override_attributes": { },
  "json_class": "Chef::Role",
  "description": "Edi's role.",
  "chef_type": "role",
  "run_list": [ 
	"recipe[fail2ban]", 
	"recipe[atop]",
	"recipe[cockpit]",
	"recipe[epel]",
	"recipe[mlocate]",
	"recipe[yum-cron]",
	"recipe[weekly-reboot]",
	"recipe[chef-repo-git-sync]",
	"recipe[ntpd-server]",
	"recipe[dnsmasq]",
	"recipe[mdns]",
	"recipe[auth-key]",
	"recipe[sshd_config]"
	]
}
